
    import 'package:flutter/material.dart';
    class Drift_sessionsPage extends StatelessWidget {
        const Drift_sessionsPage({Key? key}) : super(key: key);
      
        @override
        Widget build(BuildContext context) {
          return Scaffold(
            appBar: AppBar(
              title: Text('Drift_sessions Page'),
            ),
            );
            }
        }
    